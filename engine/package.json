{
  "name": "@ddr/meta-engine",
  "version": "0.1.0",
  "description": "Meta DDR Engine - Headless decision validation engine for DDR Control Plane",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "test:watch": "jest --watch",
    "lint": "eslint src --ext .ts",
    "clean": "rimraf dist"
  },
  "keywords": [
    "ddr",
    "decision",
    "deterministic",
    "governance",
    "validation"
  ],
  "license": "UNLICENSED",
  "dependencies": {
    "json-stable-stringify": "^1.1.1"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/json-stable-stringify": "^1.0.36",
    "@types/node": "^20.0.0",
    "jest": "^29.7.0",
    "rimraf": "^5.0.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
